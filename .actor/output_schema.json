{
    "actorOutputSchemaVersion": 1,
    "title": "Output schema of NPM Package Update Tracker",
    "properties": {
        "results": {
            "type": "string",
            "title": "Published versions",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
