{
    "name": "npm-package-tracker",
    "version": "0.1.0",
    "type": "module",
    "description": "Track new published versions and deprecation warnings for npm packages via the official npm registry API.",
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "apify": "^3.2.6"
    },
    "scripts": {
        "start": "node src/main.js"
    }
}
